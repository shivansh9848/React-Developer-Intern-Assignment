const ReportButton=()=> {
    const handleReportClick = () => {
      // Trigger report generation, route to a new page, or open a modal
    };
  
    return (
      <div className="report-button-container">
        <button onClick={handleReportClick}>Report</button>
      </div>
    );
  }
  
export default ReportButton